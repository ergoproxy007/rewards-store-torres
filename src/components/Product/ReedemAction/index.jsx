import Button from '@material-ui/core/Button';
import { RightContainer } from 'components/RightContainer';

export const ReedemAction = (props) => {
    return (
        <RightContainer>
            <Button size="small" variant="outlined" color="primary">reedem</Button>
        </RightContainer>
    );
}
